# auto-generated by grc.converter

id: FDC_WaterfallMsgTagging
label: WaterfallMsgTagging
category: '[FDC]'

parameters:
-   id: blocklen
    label: Blocklen
    dtype: int
    default: blocklen
    hide: none
-   id: samp_rate
    label: Sample rate
    dtype: float
    default: samp_rate
-   id: relinvovl
    label: Rel. inv. Overlap
    dtype: int
    default: relinvovl
-   id: blockdecimation
    label: Block Decimation
    dtype: int
    default: int(samp_rate/blocklen/8)
-   id: loginput
    label: Logarithmic input
    dtype: enum
    options: ['1', '0']
    option_labels: ['Yes', 'No']
-   id: minvaldb
    label: Minimum value[db]
    dtype: float
    default: '-30.0'
-   id: maxvaldb
    label: Maximum value[db]
    dtype: float
    default: '+30.0'
-   id: colorscheme
    label: Colorscheme
    dtype: enum
    options: ['0', '1', '2', '3']
    option_labels: [Black-Blue-Cyan-White, Black-Rainbow, Black-Red-Yellow, Black-White]
-   id: tagmode
    label: Tagmode
    dtype: enum
    options: ['0', '1', '2', '3']
    option_labels: [Only Frame, Print ID, Print part-info, Print ID and part-info]
-   id: gui_hint
    label: GUI Hint
    dtype: gui_hint

inputs:
-   domain: stream
    dtype: float
    vlen: ${ blocklen }
-   domain: message
    id: msgin

templates:
    imports: import gnuradio.FDC as FDC
    make: |-
        <%win = 'self._%s_win' % id %> \
        ${win} = FDC.WaterfallMsgTagging(${blocklen}, ${samp_rate}, ${relinvovl}, ${blockdecimation}, ${loginput}, ${minvaldb}, ${maxvaldb}, ${colorscheme}, ${tagmode})
        ${gui_hint() % win}
    callbacks:
    - set_minvaldb(${minvaldb})
    - set_maxvaldb(${maxvaldb})
    - set_colorscheme(${colorscheme})

file_format: 1
