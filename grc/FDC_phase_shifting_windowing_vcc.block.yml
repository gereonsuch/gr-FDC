# auto-generated by grc.converter

id: FDC_phase_shifting_windowing_vcc
label: Phase compensating windowing
category: '[FDC]'

parameters:
-   id: blocklen
    label: Block length
    dtype: int
    default: '256'
    hide: none
-   id: numphasestates
    label: Number of phases
    dtype: int
    default: '4'
-   id: $shifts
    label: Blockwise Phaseshift
    dtype: int
    default: '1'
-   id: passbw
    label: Normalized Pass Bw
    dtype: float
    default: '0.6'
-   id: stopbw
    label: Normalized Stop Bw
    dtype: float
    default: '0.9'
-   id: windowtype
    label: Window Type
    dtype: enum
    options: ['0', '1', '2']
    option_labels: [Rectangular, Hann, Ramp]

inputs:
-   domain: stream
    dtype: complex
    vlen: ${ blocklen }

outputs:
-   domain: stream
    dtype: complex
    vlen: ${ blocklen }
asserts:
- ${ blocklen >= 1 }

templates:
    imports: import gnuradio.FDC as FDC
    make: FDC.phase_shifting_windowing_vcc(${blocklen}, ${numphasestates}, ${shifts},
        ${passbw}, ${stopbw}, ${windowtype})

documentation: "__Phase compensating windowing__\n\nMultiplies a windowing with continous\
    \ phase shifts from block to block. \n\n * Block length is the input and output\
    \ vector length\n * Number of phases is the amount of different phase shifted\
    \ windows\n * Blockwise Phaseshift is the consecutive iteration amount\n * Normalized\
    \ Pass Bw is the total centered bandwidth to be extracted\n * Normalized Stop\
    \ Bw is the total bandwidth that is multiplied by zero\n * Window Type defines\
    \ the transition curve from stop to pass\n \nExample: \nPassBW=0.5\nStopBW=0.75\n\
    WindowType=RAMP\nblocklen=200\n\nThis equals to a Window with \nw = [  25 samples\
    \ zeros, \n       25 samples RAMP, \n      100 samples ones, \n       25 samples\
    \ RAMP, \n       25 samples zeros  ]\n\nWith NumberOfPhases=4 this leads to 4\
    \ windows, each shifted by PI/4. \nFor Shift=3, w_0 is used first, then w_3, then\
    \ w_2, then w_1 and repeat. \nFor shift 0, only the first window is used, equal\
    \ to no phase shift."

file_format: 1
